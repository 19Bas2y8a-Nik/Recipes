# Следующие шаги после успешной сборки

## ✅ Что уже сделано:
- ✅ Установлены зависимости (Auth.js, Prisma Adapter)
- ✅ Обновлена Prisma schema (добавлены таблицы для Auth.js)
- ✅ Создана конфигурация Auth.js с Google OAuth
- ✅ Создана страница `/login`
- ✅ Настроен middleware для защиты маршрутов
- ✅ Обновлены API routes для использования userId
- ✅ Проект успешно собирается

## 📋 Что нужно сделать дальше:

### 1. Применить миграции базы данных

Создайте и примените миграцию для новых таблиц Auth.js:

```powershell
# Создать миграцию
pnpm run db:migrate

# Или использовать db:push (для разработки)
pnpm run db:push
```

Это создаст таблицы:
- `accounts` - для хранения OAuth аккаунтов
- `sessions` - для хранения сессий пользователей
- `verification_tokens` - для верификации токенов

### 2. Настроить переменные окружения на Vercel

В Vercel Dashboard → Settings → Environment Variables добавьте:

```
DATABASE_URL=ваша-строка-подключения-с-pooler
AUTH_SECRET=ваш-сгенерированный-секретный-ключ
GOOGLE_CLIENT_ID=ваш-google-client-id
GOOGLE_CLIENT_SECRET=ваш-google-client-secret
```

**Важно:**
- Добавьте для всех окружений (Production, Preview, Development)
- Используйте **pooler connection** для DATABASE_URL (должно быть `-pooler` в хосте)
- После добавления переменных **обязательно передеплойте проект**

### 3. Настроить Google OAuth Redirect URI

В Google Cloud Console:
1. Перейдите в "APIs & Services" → "Credentials"
2. Откройте ваш OAuth 2.0 Client ID
3. В разделе "Authorized redirect URIs" добавьте:
   - `http://localhost:3000/api/auth/callback/google` (для локальной разработки)
   - `https://your-project.vercel.app/api/auth/callback/google` (для продакшена, замените на ваш домен)

### 4. Протестировать локально

```powershell
# Запустите dev сервер
pnpm run dev
```

Откройте http://localhost:3000 и проверьте:
- ✅ Главная страница открывается
- ✅ Кнопка "Войти" ведет на `/login`
- ✅ Страница `/login` отображается
- ✅ Кнопка "Войти через Google" работает
- ✅ После входа отображается информация о пользователе
- ✅ Кнопка "Выйти" работает

### 5. Закоммитить изменения и задеплоить

```powershell
# Проверьте изменения
git status

# Добавьте файлы
git add .

# Создайте commit
git commit -m "Add Google OAuth authentication with Auth.js"

# Отправьте в репозиторий
git push
```

Vercel автоматически задеплоит проект после push.

### 6. Применить миграции на продакшене

После деплоя на Vercel примените миграции:

**Вариант 1: Через Vercel CLI**
```powershell
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Подключите проект
vercel link

# Примените миграции
vercel env pull .env.local
pnpm exec prisma migrate deploy
```

**Вариант 2: Через Neon Dashboard**
1. Откройте [Neon Dashboard](https://console.neon.tech)
2. Перейдите в SQL Editor
3. Выполните SQL из файла миграции вручную

### 7. Проверить работу на продакшене

После деплоя проверьте:
- ✅ Главная страница открывается: `https://your-project.vercel.app`
- ✅ Страница входа работает: `https://your-project.vercel.app/login`
- ✅ Аутентификация через Google работает
- ✅ После входа отображается информация о пользователе
- ✅ Защищенные маршруты (`/dashboard`, `/my-prompts`) требуют авторизации

## 🔍 Проверка работы

### Локально:
1. Откройте http://localhost:3000
2. Нажмите "Войти"
3. Нажмите "Войти через Google"
4. Авторизуйтесь через Google
5. Проверьте, что вы видите свое имя и кнопку "Выйти"

### На продакшене:
1. Откройте ваш сайт на Vercel
2. Повторите те же шаги
3. Убедитесь, что все работает корректно

## ❓ Если что-то не работает:

### Проблема: "AUTH_SECRET is not set"
- **Решение:** Добавьте `AUTH_SECRET` в переменные окружения Vercel

### Проблема: "OAuth callback error"
- **Решение:** Проверьте, что redirect URI добавлен в Google Cloud Console

### Проблема: "Database connection error"
- **Решение:** 
  - Проверьте, что используется pooler connection для DATABASE_URL
  - Убедитесь, что база данных не заморожена в Neon

### Проблема: Таблицы не созданы
- **Решение:** Примените миграции через `pnpm run db:migrate` или через Neon Dashboard

## 📚 Полезные ссылки:

- [ENV.md](./ENV.md) - инструкции по настройке переменных окружения
- [VERCEL_DEPLOY.md](./VERCEL_DEPLOY.md) - инструкции по деплою на Vercel
- [BUILD_TROUBLESHOOTING.md](./BUILD_TROUBLESHOOTING.md) - решение проблем со сборкой
