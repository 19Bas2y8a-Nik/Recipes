# Как проверить переменные окружения в Vercel

## Пошаговая инструкция

### 1. Откройте проект в Vercel Dashboard

1. Зайдите на [Vercel Dashboard](https://vercel.com/dashboard)
2. Найдите и откройте ваш проект

### 2. Перейдите в настройки переменных окружения

1. В верхнем меню проекта нажмите **Settings**
2. В левом меню выберите **Environment Variables**

### 3. Проверьте список переменных

Вы увидите таблицу со всеми переменными окружения. Для каждой переменной отображаются:

- **Name** - название переменной
- **Value** - значение (скрыто, показывается только при редактировании)
- **Production** - чекбокс для production окружения
- **Preview** - чекбокс для preview окружения  
- **Development** - чекбокс для development окружения
- **Actions** - кнопки для редактирования/удаления

### 4. Проверьте каждую переменную

Убедитесь, что для всех 4 переменных отмечены все три чекбокса:

```
✅ DATABASE_URL          [☑ Production] [☑ Preview] [☑ Development]
✅ AUTH_SECRET           [☑ Production] [☑ Preview] [☑ Development]
✅ GOOGLE_CLIENT_ID      [☑ Production] [☑ Preview] [☑ Development]
✅ GOOGLE_CLIENT_SECRET  [☑ Production] [☑ Preview] [☑ Development]
```

### 5. Если чекбоксы не отмечены

**Вариант 1: Редактирование существующей переменной**
1. Нажмите на переменную (или кнопку редактирования ✏️)
2. В открывшемся окне отметьте нужные окружения:
   - ☑ Production
   - ☑ Preview
   - ☑ Development
3. Нажмите **Save**

**Вариант 2: Добавление новой переменной**
1. Нажмите **Add New** или **Add**
2. Введите:
   - **Key**: название переменной (например, `AUTH_SECRET`)
   - **Value**: значение переменной
3. Отметьте окружения:
   - ☑ Production
   - ☑ Preview
   - ☑ Development
4. Нажмите **Save**

## Визуальная проверка

После правильной настройки таблица должна выглядеть так:

| Name | Production | Preview | Development | Actions |
|------|------------|---------|-------------|---------|
| DATABASE_URL | ☑ | ☑ | ☑ | ✏️ 🗑️ |
| AUTH_SECRET | ☑ | ☑ | ☑ | ✏️ 🗑️ |
| GOOGLE_CLIENT_ID | ☑ | ☑ | ☑ | ✏️ 🗑️ |
| GOOGLE_CLIENT_SECRET | ☑ | ☑ | ☑ | ✏️ 🗑️ |

## Важные замечания

1. **Переменные применяются только к новым деплойментам**
   - После добавления/изменения переменных нужно передеплоить проект
   - Старые деплойменты не получат новые переменные автоматически

2. **Разные значения для разных окружений**
   - Если нужно использовать разные значения для Production и Development, добавьте переменную дважды с разными значениями
   - Например: `DATABASE_URL` для Production и `DATABASE_URL` для Development

3. **Безопасность**
   - Значения переменных скрыты в интерфейсе
   - Для просмотра значения нужно нажать на переменную
   - Sensitive переменные (пароли, секреты) автоматически скрываются

## Проверка через Vercel CLI

Вы также можете проверить переменные через командную строку:

```powershell
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Подключите проект
vercel link

# Просмотрите переменные окружения
vercel env ls
```

Это покажет список всех переменных с указанием, для каких окружений они настроены.

## После изменения переменных

**Обязательно передеплойте проект:**

1. В Vercel Dashboard → **Deployments**
2. Нажмите на три точки (⋮) у последнего деплоймента
3. Выберите **Redeploy**
4. Или сделайте новый commit и push в Git
